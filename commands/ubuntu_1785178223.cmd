FROM: ubuntu
TIME: 2026-07-27 18:51 UTC
CHANNEL: github-relay
PRIORITY: HIGH

REQUEST: Upgrade opencode CLI on ubuntu to PRO MAX.

We have:
• blind-proxy :8090 with 523 models
• 8 model providers (freellmapi, freebuff, fugu-proxy, mistral, groq, hf, sambanova, cerebras)
• 28 MCP tools
• 41 subagent types
• opencode v4.x at /home/ricos/.config/opencode
• AGENTS.md with full routing map

We want opencode to be the MOST POWERFUL CLI IN THE WORLD:
• Full AI intelligence (all models, all providers)
• Self-evolving (auto-discovers new models/providers)
• God-tier routing (always picks the best model)
• Autonomous upgrades
• True PRO - no limits, full power

Send config changes, agent updates, model routes, or upgrade scripts.
We will execute immediately.

STATUS: eon Pro Max = DONE. Now opencode Pro Max needed.
